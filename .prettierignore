dist
.astro
bun.lock

# planning docs — prose, hand-formatted. reformatting churns the map and
# mangles the research tables.
.scratch

# generated by resume/yaml2json.py. ci rebuilds it and byte-compares against
# what's committed, so prettier reformatting it would make that check fail on
# every push — the two would rewrite the same file forever, in opposite
# directions. the generator owns this file.
public/resume.json

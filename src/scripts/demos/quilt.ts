/** STUB — an agent fills this file and owns it entirely. `register()` is the
 * only export; index.astro already calls it. Until then, the act does not
 * exist, which costs nothing. */
export function register(): void {}
